<h2>Добро пожаловать в PizzaShop!</h2>

<div class="row">
	<% @products.each do |p| %>
		<div class="col-md-3">
			<%= p.title %>
		</div>
	<%end%>
</div>
<button class="btn btn-primary" onclick="something()">Нажми на меня!</button>
